pipeline {
    agent any
    stages {
        stage ('Git Commit') {
            steps {
                 echo "test"
           }
        stage('maven build '){
            steps {
              sh 'maven --version'
        }
    }
}
