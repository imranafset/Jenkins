pipeline {
    agent any
    stages {
        stage ('Git Commit') {
            steps {
                 git branch: 'main', url: 'https://github.com/imranafset/Jenkins.git'
           }
        }
    }
}
