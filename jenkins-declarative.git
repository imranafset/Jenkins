pipeline {
    agent any
    stages {
        stage ('Git Commit') {
            steps {
                 https://github.com/imranafset/Jenkins/blob/main/jenkins-declarative.git
           }
        }
    }
}
